%tr
  %td
    = link_to lesson.name, course_lesson_path(lesson.course, lesson)
  - if lesson_progress_finished?(lesson)
    %td
      Yes
    %td
      #{distance_of_time_in_words_to_now(@completion.created_at)} ago
  - else
    %td
      Not yet!
    %td